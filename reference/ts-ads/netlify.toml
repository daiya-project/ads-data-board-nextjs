# Netlify 배포 설정 (Vite + TypeScript)
# 이 파일이 있으면 푸시 시 항상 동일한 방식으로 빌드·배포됩니다.

[build]
  command = "npm run build"
  publish = "dist"

# SPA: 모든 경로를 index.html로 (필요 시)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
